Despite that GrendTrekk was initially a very different idea for a project, to say let's go back to the time, Java rock all the world, so Spring Webflux wasn't a bad idea (it_was_a_bad_idea.jpg), sadly for what GrendTrekk requires (to say, a minimal runtime, clear boundaries, and a right goddamn transactional security!) things that Java at R2DBC by the time (July 2025) didn't provided, and as I'd run tired for dealing with Java's issues, I'd switched out torns, seeking out in Rust a new option (why? easy, because Rust was the only viable alternative for a low-level tool grade, since damn! I'd didn't bet for JavaScript, and Python async thing isn't something that make's me enthusiastic); so the venture begun (as grendtrekk-protocol, not grendtrekk-prototype), intially all waw Rust dealing, and that wasn't all that bad, issue? well that the boundaries wasn't clear enough, all was a monolithc single query to get the columns and from that rebuild all, and not even so, to make all worse, it was from the 'view' 'INFORMATION_SCHEMA', thing that made things harder at that point to be rightfull, worse since I'd was expecting to do Java but in Rust (again; bad_idea.jpg), so Rust was all that cool, doing async database connections (not pooling yet!) all what I'd wanted in Java, I'd held it in Rust, so now that I'd got the Empire by it's throat, was time to restart all from scratch (costly_idea.jpg); but that restart and do more idiomatic Rust was better than pretending to do Java as Rust, as many things really change for good from Java to Rust, and what is better now is to actually deal with the weight of it, so by now things are getting complex, since the sys schema is merciless, rude and deliberatedly shadowed, a very usefull and edge piece at SQL Server internals that is underseen, but when is a matter to do DBA investigations, it's the Rosseta Stone to cypher all those connections and weird things inside the engine, why I'd mention it, well applies here. 

to get thigns clear GrendTrekk's main goal is to translate databases from SQL Server to PostgreSQL, with the minimal human intervention, using a Cannonical model, to actually become universal (as the same idea of sys spawns across other database engines, with changes, but the idea is to gather the primordial data at one place, and then re-use it across database engines) that later on the issue is to implement a buffer to held part of the data (from origin) while the migration is getting done, so that's the whole idea, sounds simple but it isn't, and it isn't due to the issue of how much edge cases are inside SQL Server, and mostly how those should be handled in PostgreSQL (like collation, indexes, sentences, types and computed columns, to name a few). 

so by now I'm putting this project at CONCURRENT with other projects of higher importance for me, so this isn't a farewell, it's a promise of return, as I'd returned from July 2025, I'll return to this point (March 26, 2026) soon in the future, so this is a work, in progress ...
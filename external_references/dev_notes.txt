-- From what is made a SQL DDL column? 

<column_name> <data_type>(<length_field>) 
(data_type.eq(string_rel)? COLLATION : EMPTY) 
<is_nullable>([NOT NULL]? true : false) 


PG as ALTER : 
ALTER TABLE ONLY users ALTER COLUMN lang SET DEFAULT 'en_GB';


examples: 


------ 
Columns 
------
--MSSQL
BusinessEntityID int NOT NULL,
PersonType nchar(2) COLLATE SQL_Latin1_General_CP1_CI_AS NOT NULL,
NameStyle NameStyle DEFAULT 0 NOT NULL,

-- PG
  name TEXT NOT NULL COLLATE "en_US.UTF-8", 


-----
PK
-----
CONSTRAINT PK_Person_BusinessEntityID PRIMARY KEY (BusinessEntityID),

-- Translation (to PG):

-* as Identity: 
Id integer GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) PRIMARY KEY 


ex: 
"CustomerID" INTEGER GENERATED BY DEFAULT AS IDENTITY (START WITH 30118 INCREMENT BY 1) PRIMARY KEY


-* non Identity: (correction: on non Identity, the follow is correct, but it isn't for the long run when many fields are gathered upon a primary key; so here's the next step) 
OrderCode numeric(10,0) PRIMARY KEY

Or : 

CREATE TABLE example (
    a integer,
    b integer,
    c integer,
    constraint tester_PK_a PRIMARY KEY (a, c)
);

here count the fields marked as primary key's then over that decide when to do this: OrderCode numeric(10,0) PRIMARY KEY
or the previous statement arragement as :  constraint tester_PK_a PRIMARY KEY (a, c)



-----
FK
-----
CONSTRAINT FK_Person_BusinessEntity_BusinessEntityID FOREIGN KEY (BusinessEntityID) REFERENCES AdventureWorks2022.Person.BusinessEntity(BusinessEntityID)

---------
Collations 


CREATE COLLATION "SQL_Latin1_General_CP1_CI_AS" (
    PROVIDER = 'icu',
    LOCALE = 'en-u-ks-level2',
    DETERMINISTIC = false
);

also remember about 

ICU =  International Components of Unicode Library !

The anatomy of an ICU locale string
<language>-<region>-u-<key>-<value>-<key>-<value>...
Example: en-US-u-kn-true-ks-level2

en-US —> the base language tag (BCP 47): language + optional region/script
-u- —> introduces Unicode Consortium-specific options
kn-true, ks-level2, etc. —> the "keyword" options tacked on after -u-